<div class="container">
    <h1 class="text-center">Administration manager</h1>

    <nav class="nav justify-content-center">
      <a class="nav-link" href="{{ path('admin_prestations') }}">Prestations management</a>
      <a class="nav-link" href="{{ path('admin_status') }}">Status management</a>
      <a class="nav-link" href="{{ path('admin_taxes') }}">Taxes management</a>
      {# <a class="nav-link" href="{{ path('admin_users') }}">Users management</a>
      <a class="nav-link" href="{{ path('admin_reservations') }}">Reservations management</a> #}
    </nav>
</div>