{% extends 'base.html.twig' %}

{% block body %}
    <div class="d-flex staff-show">
        <div>
            <img width="300px" src="/img/avatar/{{user.avatar}}" alt="{{ user.firstName }}">
        </div>
        <div class="ml-3">
            <h2>{{ user.firstName }}</h2>
            <p>{{ user.gender }}</p>
            <p>{{ user.age }}</p>
        </div>
    </div>

{% endblock %}
